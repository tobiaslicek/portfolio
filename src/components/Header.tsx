export const Header = ({ name, contacts }) => {
  return (
    <header className="cv-header">
      <h1 className="cv-name">Jan Novák</h1>
      <p className="cv-contact"></p>
      <img className="cv-photo" src="#" alt="Profile Picture"></img>
    </header>
  );
};
